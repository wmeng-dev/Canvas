# 发散创意画布桌面应用 · 产品技术方案 v0.1

> 定位：一个**独立桌面应用**，把 AI 创意发散做成"可视化创意树"，并通过 **MCP 协议**对接任意 AI 工具。
> 业务内核对标 Ramify（DeepSeek Harness 的创意画布插件），但脱离对特定 agent 框架的依赖，成为通用的、本地优先的桌面创作环境。
> 文档状态：方案草案，关键决策点已标注"⚠️ 需拍板"。

---

## 1. 背景与产品定位

### 1.1 痛点（与 Ramify 一致）
创意类任务（UI 样式、文案、视觉、交互感觉、产品结构）没有标准答案，"看到才知道对不对"。传统"描述 → 生成一版 → 在对话里改"的姿势等于闭眼找东西——只看到单一方案，被迫在第一个坑里深挖。正确姿势是**先把方向铺开（发散），再收敛定稿**。

### 1.2 与 Ramify 的差异

| 维度 | Ramify（参考） | 本应用（目标） |
|---|---|---|
| 形态 | DSH 插件，依赖 DeepSeek Harness 运行时 | **独立桌面应用**，双击即用，无外部框架依赖 |
| AI 后端 | 复用 DSH 当前会话的模型，绑定单一框架 | **通过 MCP 对接任意 AI 工具/模型**（OpenAI、Claude、DeepSeek、本地 Ollama、专业 agent 框架…） |
| 可移植性 | 随 DSH 生态 | 自带完整的创作+存储+导出闭环，可离线 |
| 扩展性 | 受 DSH 插件机制约束 | 作为 MCP Host 接入任意 MCP Server；同时自身可暴露为 MCP Server（双向） |
| 数据归属 | 跟随 DSH | **本地优先**，数据存本机，隐私可控 |

### 1.3 核心价值主张
> **"把 AI 创意发散做成一棵可以随手展开、对比、翻案的树，并且树上的每个节点都能用你手里任何 AI 工具来生成。"**

---

## 2. 核心使用流程

```
[输入需求] → 选 AI 后端 + 方案数量 N
        │
        ▼
[根节点生成] 一次并行长出 N 个子方案（每个是树节点，实时渲染预览）
        │
        ▼
[对比 / 挑选] 在画布上并排看 N 版，点开即预览（HTML / Markdown / SVG / 图片）
        │
        ▼
[继续发散] 从任一节点再长出子节点（每轮修订=新子节点，旧版完整保留，可随时翻案）
        │
        ▼
[定稿] 选定最终方案 → 导出规格（Markdown / 单文件 HTML / SVG）→ 交给 AI 或人去实现
```

**适用场景**：产品原型、UI/交互方案、落地页、品牌文案、内容结构、SVG 图形、文档大纲——凡"看到才知道对不对"的创作。

---

## 3. 功能设计（MVP 起）

| 模块 | 功能 | 说明 |
|---|---|---|
| 创意树画布 | 节点图、缩放/平移、连线、拖拽 | 基于 React Flow |
| 节点生成 | 选 AI 后端 + 提示词 → 生成内容 | 支持一次生成 N 个并行子节点 |
| 节点预览 | 可交互 HTML（沙箱 iframe）、Markdown、SVG、图片 | 多类型内容原生渲染 |
| 发散/收敛 | 从任意节点继续发散，旧版本保留 | 树形版本管理，支持"翻案" |
| AI 后端管理 | 配置/切换 AI 来源（直连 API 或 MCP Server） | 可视化配置 |
| 本地存储 | 项目/树/节点持久化 | SQLite + 资源文件目录 |
| 导出 | 导出最终方案为独立文件 | Markdown / 单文件 HTML（含内联 CSS/JS）/ SVG |
| MCP 接入 | 连接任意 MCP Server 作为生成/工具来源 | 重点差异化能力 |

---

## 4. 技术架构

```
┌──────────────────────────────────────────────────────────┐
│                      桌面应用（Host）                        │
│  ┌────────────┐  ┌────────────┐  ┌─────────────────────┐  │
│  │ 创意树画布  │  │ 节点预览引擎 │  │  AI 后端管理 UI      │  │
│  │ (React Flow)│  │(iframe/MD/SVG)│ │（直连 / MCP 配置）   │  │
│  └─────┬──────┘  └─────┬──────┘  └──────────┬──────────┘  │
│        └───────────────┴─────────────────────┘            │
│                          │                                 │
│            ┌─────────────▼──────────────┐                 │
│            │   统一生成抽象层 Generator    │                 │
│            │   generate(nodeSpec) → content│                │
│            └──────┬───────────────┬──────┘                 │
│                   │               │                        │
│         ┌─────────▼───┐   ┌───────▼─────────┐              │
│         │ AI Provider  │   │  MCP Client      │             │
│         │ Adapter 层   │   │  Manager          │            │
│         │(直连 REST API)│   │(stdio / HTTP)     │           │
│         │ OpenAI/      │   │ 连接任意 MCP Server│           │
│         │ Anthropic/   │   └───────┬─────────┘             │
│         │ DeepSeek/    │           │                       │
│         │ Ollama       │           │   ┌────────────────┐  │
│         └──────────────┘           ├──▶│ MCP Server A    │  │
│                                     ├──▶│ (某 AI 工具封装)│  │
│                                     └──▶│ MCP Server B    │  │
│                                         │ (filesystem…)   │  │
│                                         └────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ 本地存储层：SQLite（树/节点元数据）+ 资源目录（产物文件）│ │
│  └──────────────────────────────────────────────────────┘ │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ （可选·未来）本应用暴露为 MCP Server，对外提供创意树资源 │ │
│  └──────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
```

**关键设计点**：无论 AI 来自"直连 API"还是"某个 MCP Server 里的 AI 工具"，对画布来说都透过统一的 `Generator.generate(nodeSpec)` 抽象调用。新增一个 AI 工具 = 接入一个 MCP Server 或加一个 adapter，**画布代码零改动**。

---

## 5. 桌面框架选型（⚠️ 需拍板）

| 框架 | 优点 | 缺点 | 适配度 |
|---|---|---|---|
| **Electron** | 前端栈（Chromium+Node）、生态最成熟、官方 `@modelcontextprotocol/sdk` 是 TS、MCP stdio 子进程管理方便、iframe 预览天然支持 | 包体大、内存占用高、需注意安全 | ⭐⭐⭐⭐⭐（**MVP 推荐**） |
| **Tauri** | 包小、性能高、安全模型好、Rust 后端 | Rust 学习/维护成本；MCP 进程管理需 Rust；WebView 一致性问题 | ⭐⭐⭐⭐（长期推荐） |
| **Wails** | Go + WebView，类似 Tauri 但用 Go | Go 生态、WebView 一致性同问题 | ⭐⭐⭐ |

**推荐**：MVP 阶段用 **Electron** 快速验证（你前端背景契合、MCP SDK 现成、iframe 渲染可信内容省心）；架构上把"AI 调度 / MCP / 存储"做成与 UI 解耦的核心模块，未来若在意体积/性能可平移到 Tauri。

---

## 6. MCP 集成设计（核心差异化）

### 6.1 作为 MCP Host（接入任意 AI 工具）
- 内置 **MCP Client Manager**：用户可视化添加 MCP Server（本地 stdio 进程，或远程 Streamable HTTP / SSE）。
- 每个 AI 工具只要被封装成一个暴露 `generate_*` / `analyze` 等 tool 的 MCP Server，本应用即可作为后端调用——**无需为某家 AI 写特例代码**。
- 典型形态：社区提供 `openai-mcp-server`、`claude-mcp-server`、`deepseek-mcp-server`、`ollama-mcp-server`，把各家能力暴露成统一 tool；用户装好即在"AI 后端"列表里看到它。

### 6.2 自身暴露为 MCP Server（双向，未来）
- 把创意树、节点内容作为 **Resource** 暴露，把"从某节点发散""对比两节点"作为 **Tool/Prompt** 暴露。
- 好处：别的 AI 工具（如你的 IDE agent、DSH、Claude Desktop）可直接读取/操作你的创意树，形成"桌面画布 ↔ 任意 AI 工具"双向闭环。这是超越 Ramify 的关键点。

### 6.3 安全防护
- MCP Server 在**沙箱/独立进程**运行，最小权限；stdio Server 的白名单与命令校验。
- 渲染 AI 生成的 HTML 用**沙箱 iframe**（`sandbox` 属性 + 禁 script），避免恶意内容影响宿主。

---

## 7. 统一生成抽象层（关键接口草图）

```ts
// 对画布暴露的统一接口 —— 屏蔽"直连 API"与"MCP Server"的差异
interface Generator {
  id: string;
  label: string;            // 在 UI 里显示的 AI 后端名
  kind: 'direct' | 'mcp';   // 来源类型
  generate(spec: NodeSpec): Promise<NodeContent>;
}

interface NodeSpec {
  prompt: string;           // 用户需求 / 上一节点内容
  contentType: 'html' | 'markdown' | 'svg' | 'image' | 'text';
  variationSeed?: number;   // 用于并行生成多版时的差异
  context?: NodeRef[];      // 可选：父节点/兄弟节点上下文
}

// DirectAdapter：直连 OpenAI/Anthropic/DeepSeek/Ollama REST
// McpAdapter：调用某 MCP Server 的 generate tool，结果归一化为 NodeContent
```

---

## 8. 数据模型（SQLite）

```
Project(id, name, createdAt)
Tree(id, projectId, rootNodeId, title)
Node(id, treeId, parentId, title, contentType,
     content, promptSpec, aiSourceId, version,
     createdAt, status)        -- 每轮发散=新 Node，旧版保留
Edge(id, treeId, fromNodeId, toNodeId, kind)   -- 父子发散关系
AISourceConfig(id, label, kind, creds/json, isActive)  -- 直连凭证/或 mcp server 引用
MCPServerConfig(id, name, transport, command/url, args, env, enabled)
```

产物资源（HTML/SVG/图片二进制）存本地目录 `~/AppData/<app>/projects/<pid>/nodes/<nid>/`，DB 只存路径。

---

## 9. 技术栈建议

| 层 | 选型 |
|---|---|
| 桌面壳 | Electron（MVP）→ Tauri（长期评估） |
| 前端框架 | React + TypeScript + Vite |
| 画布引擎 | React Flow（@xyflow/react） |
| 状态管理 | Zustand（轻量，适合画布） |
| 本地存储 | SQLite（better-sqlite3 / sql.js）+ 资源文件目录 |
| MCP SDK | `@modelcontextprotocol/sdk`（TS 官方） |
| 直连 AI | 各厂商 REST（OpenAI / Anthropic / DeepSeek / Ollama 兼容接口） |
| 预览 | 沙箱 iframe（HTML）、react-markdown、内联 SVG、<img> |
| 导出 | 单文件 HTML（内联 CSS/JS）、Markdown、SVG |

---

## 10. 实施路线图

- **Phase 0 · 技术验证（1–2 周）**
  - Electron 空壳跑通；MCP Client 连通一个本地 echo/demo MCP Server；直连一个模型（如 DeepSeek）生成单个文本节点并渲染。
- **Phase 1 · MVP（4–6 周）**
  - 创意树画布 + 节点生成（一次 N 个并行）+ 多类型预览 + 本地存储 + 导出。
  - AI 后端：先支持 1 个直连（DeepSeek/OpenAI）+ 1 个示例 MCP Server。
- **Phase 2 · MCP 生态（后续）**
  - 可视化 MCP Server 配置与状态监控；内置常见 AI 工具 MCP Server 一键接入引导。
- **Phase 3 · 双向 & 协作**
  - 本应用暴露为 MCP Server；可选云同步（端到端加密）；多画布对比。
- **Phase 4 · 高级能力**
  - 节点版本 diff、AI 自动评审/打分、行业模板库（含农业 SaaS 原型模板）。

---

## 11. 风险与缓解

| 风险 | 缓解 |
|---|---|
| MCP Server 质量/安全参差不齐 | 沙箱进程 + 白名单 + 最小权限；社区 Server 需签名/审核机制 |
| 多 AI 后端输出格式不一致 | 统一 `NodeContent` 归一化层；contentType 约束 |
| Electron 渲染不可信 HTML | 沙箱 iframe，禁 script；资源目录隔离 |
| MCP 协议演进（传输层/原语变化） | 锁定 SDK 版本，抽象层隔离协议细节 |
| 创意树数据量大导致画布卡顿 | 节点懒加载 + 视口裁剪 + 虚拟化 |

---

## 12. ⚠️ 待拍板决策点（我的推荐 + 理由）

1. **桌面框架**：推荐 **Electron（MVP）**，长期评估 Tauri。→ 拍板？
2. **目标平台优先级**：默认 **Windows 优先**（你在 Win 环境），是否也要 macOS / Linux 包？
3. **MVP 首发 AI 后端**：推荐 **DeepSeek（直连）+ 一个示例 MCP Server**。是否要同时接 OpenAI/Claude？
4. **节点内容类型优先级**：MVP 先支持 **HTML + Markdown + SVG**，图片生成放 Phase 2？
5. **是否现在就做"自身暴露 MCP Server"**：建议 **Phase 3** 再做，MVP 聚焦"作为 Host 接入"。

> 以上决策点确认后，我可据此产出 Phase 0/1 的详细实现计划（含目录结构、关键模块代码骨架、里程碑）。
