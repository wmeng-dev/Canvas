# Dependencies
node_modules/
# Build output
dist/
dist-electron/
dist-test/
# electron-builder output (installer / unpacked app)
release/
# electron-builder 的其它输出目录（release-win-20260922-1201 / release-tmp / release-clean …）。
# ⚠️ 用 `release-*/` 族匹配而不是逐个列：漏一个目录，`git add -A` 就会把 ~180MB 的 exe 带进提交。
# 也别只写 release-win-*/ —— 临时验证目录常起别的名字（如 release-check-*）。
release-*/
# WorkBuddy project data (personal config, do NOT commit)
.workbuddy/
# Local data / secrets
*.db
*.db-wal
*.db-shm
userData/
.env
.env.*
# Logs / temp probes
*.log
.a*.log
# 回归/类型检查的日志落盘（Windows 上 stdout 常丢，只能写文件再读）
*.out.txt
# Local verification artifacts (electron screenshots)
/canvas-screenshot.png
/export-html-screenshot.png
/ui-*.png
# ⚠️ 探针**已纳入版本控制**：tests/e2e/probe-*.cjs
# 这里原本有一条 `probe-*.cjs` 忽略规则，结果是整个 UI 回归套件只存在于本机：
# 新克隆的仓库零 UI 验证、另一台机器拿不到探针修复。别再把它加回来。
# OS
Thumbs.db
Desktop.ini
.DS_Store

# CI 工作流模板放在 docs/ci/（真正的 .github/workflows 需要 PAT 的 workflow scope 才能推，
# 本机缓存的 token 没有该 scope，一推就会被整批拒回）。别用 git add -A 把它带回来。
/.github/
