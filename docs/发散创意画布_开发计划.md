# 发散创意画布桌面应用 · 开发计划（小步迭代版）

> 配套文档：`docs/发散创意画布桌面应用_方案.md`（纲领）、`docs/发散创意画布_实现计划_Phase0-1.md`（架构与代码骨架）
> 本文件是**执行层面的开发计划**，核心原则：**一个功能一步、做一步验证一步、每步本地提交、确认后再上传。**

---

## 0. 总原则与节奏

1. **极小步**：每个步骤只完成"一个可独立验证的功能点"，不贪大。
2. **先验证后继续**：每步做完，先按"验证方法"自测通过，再进入下一步。
3. **小步提交**：每步自测通过后，`git commit` 到本地 `main`（提交信息带步骤号，如 `step 3.2 deepseek adapter`）。
4. **确认后上传**：**不自动 push**。每完成一个阶段（或你口头确认"这步 OK"）后，才 `git push origin main`。
5. **可回退**：任一步验证失败，立即停下汇报，不强行往下做；必要时 `git revert` 该步提交回到干净状态。

---

## 1. 仓库与 Git 工作流

- **远程仓库**：`https://github.com/wmeng-dev/Canvas.git`（推断已存在，可能为空仓库；第 0 步核实）
- **本地仓库根**：`E:\Canvas`（即当前工作区，作为项目根）
- **默认分支**：`main`
- **目录约定**：
  - 应用代码在仓库根（`package.json`、`src/main`、`src/core`、`src/renderer` 等）
  - 文档归入 `docs/`（本文件及两份方案文档将移入 `docs/`）
- **`.gitignore`**（第 0 步创建，必须忽略）：
  ```
  node_modules/
  dist/
  dist-electron/
  .workbuddy/
  *.db
  *.db-wal
  *.db-shm
  userData/
  *.log
  .env
  ```
  > ⚠️ `.workbuddy/`、`.env`、`*.db`、`userData/` 含个人配置/密钥/本地数据，**严禁上传**。
- **提交粒度**：每步一提交；禁止把多步混在一个大提交里。
- **推送时机**：阶段 A/B/C/D 各自完成后（或你逐步确认后）`git push`；首次 `git push -u origin main`。
- **前置检查**：push 前确认本机已登录 GitHub（`gh auth status` 或 Git Credential Manager 已缓存令牌）。

---

## 2. 环境前置检查（第 0 步先做）

- [ ] Node 可用（managed 22）：`node -v`
- [ ] 包管理器可用：`pnpm -v` 或 `npm -v`
- [ ] Git 可访问 GitHub：`git ls-remote --heads https://github.com/wmeng-dev/Canvas.git`
- [ ] 本机已登录 GitHub（可推送）：`gh auth status`
- [ ] 若仓库不存在：`gh repo create wmeng-dev/Canvas --public`（或请你在网页创建）

---

## 3. 步骤清单（极细化）

> 每步格式：**目标 → 动作 → 验证 → 提交**

### 阶段 A · 工程地基

**A.0 仓库初始化**
- 目标：本地 `E:\Canvas` 成为 git 仓库并连上远程。
- 动作：
  1. `git init`（若未初始化）
  2. `git remote add origin https://github.com/wmeng-dev/Canvas.git`（已存在则 `git remote set-url`）
  3. 创建 `.gitignore`（见上）
  4. 将两个方案 md 移入 `docs/`
  5. 写 README.md（一行说明 + 仓库用途）
- 验证：`git remote -v` 显示 origin；`git status` 干净（忽略项不出现）。
- 提交：`step A.0 init repo & gitignore`

**A.1 package.json + tsconfig + 空目录骨架**
- 目标：工程元数据与 TypeScript 基线就位，目录占位。
- 动作：写 `package.json`（name/version/scripts 占位）、`tsconfig.json`、`tsconfig.node.json`；建 `src/main`、`src/core`、`src/renderer`、`src/shared` 空目录（各放 `.gitkeep`）。
- 验证：`cat package.json` 结构正确；`tsc --noEmit` 不报配置错误（空文件）。
- 提交：`step A.1 scaffold package/tsconfig/dirs`

**A.2 安装基础依赖**
- 目标：核心依赖装好且能解析。
- 动作：`pnpm add` electron react react-dom @xyflow/react zustand better-sqlite3 @modelcontextprotocol/sdk react-markdown；`pnpm add -D` vite @vitejs/plugin-react typescript @types/* electron-builder。
- 验证：`pnpm list` 能看到这些包；`node -e "require('electron')"` 不报缺失（或用 `pnpm why`）。
- 提交：`step A.2 install deps`

### 阶段 B · 核心能力（无界面也可验证）

**B.1 Electron 空壳能启动** ✅（代码完成，已提交 `c54db8c`）
- 目标：运行后弹出窗口并显示一个占位页面。
- 动作：`src/main/main.ts` 创建 `BrowserWindow`（开发模式轮询 Vite 5173 端口就绪再 `loadURL`）；`src/renderer/index.html` + `main.tsx` 渲染 `<div>Diverge OK</div>`；`vite.config.ts` 配 renderer；`package.json` 加 `dev` 脚本（`build:main` + vite + electron）。
- 验证：`tsc` 类型检查 + `build:main` 编译均通过（已验证）。**窗口弹出需在 Windows 真机 `npm run dev` 确认**——本沙箱无 GUI/显示，Electron 运行时未注册内置 `electron` 模块（`require('electron')` 返回路径字符串而非 API），属环境限制，非代码缺陷。
- 提交：`B.1 Electron shell scaffold: main process + renderer placeholder (Diverge OK)`（`c54db8c`）

**B.2 存储层：JSON 文件存储初始化** ✅（代码完成，待提交 B.2/B.3）
- 目标：应用启动后建立本地 JSON 存储目录与数据模型（替代 SQLite）。
- 动作：
  - `src/shared/types.ts`：定义 `Project / TreeNode / TreeEdge / ProjectFile / NodeStatus` 领域类型（主/渲染进程共用，纯类型无运行时依赖）。
  - `src/core/storage/store.ts`：基于 `fs` 的 `JsonStore` 低层存储，目录 `<baseDir>/projects/<id>.json`；写操作采用「临时文件 + rename」原子替换；项目 id 安全化防路径穿越。每个项目一个 JSON 文件，结构 `{ project, tree:{ nodes, edges } }`。
- 验证：`tsc` 编译通过；Node 运行时测试 `tests/storage.test.cjs`（11 项断言全过）。
- 提交：`step B.2 json store init`

**B.3 存储层：基础 CRUD** ✅（代码完成，待提交 B.2/B.3）
- 目标：能插入并读回 project / tree / node / edge，并支持更新、删除与级联清理。
- 动作：`src/core/storage/repositories.ts` 实现 `ProjectRepository`：
  - 项目：`create / get / list / rename / remove`
  - 节点：`addNode`（自动按 parentId 建边）/ `updateNode` / `removeNode`（级联清理相关边）
  - 边：`addEdge`（拒绝自环 / 重复 / 悬空端点）/ `removeEdge`
- 验证：`tests/storage.test.cjs` 覆盖上述全部路径，11 项断言全部通过（`node node_modules/typescript/bin/tsc -p tsconfig.test.json && node tests/storage.test.cjs`）。
- 提交：`step B.3 storage repositories crud`

**B.4 Generator 抽象层 types**
- 目标：定义统一接口，后续 adapter 都实现它。
- 动作：`src/core/generator/types.ts` 写 `ContentType / NodeSpec / NodeContent / Generator / GeneratorRegistry`。
- 验证：`tsc` 编译通过（纯类型，无运行逻辑）。
- 提交：`step B.4 generator types + registry`

**B.5 DeepSeek 直连 adapter** ✅（代码完成，待提交）
- 目标：给定 prompt 能返回文本。
- 动作：`src/core/generator/direct/deepseek.ts` 实现 `DeepSeekGenerator`（实现 `Generator` 接口）：`apiKey` 由调用方注入（本地配置/环境变量，绝不硬编码、不入库）；`fetch` 调 `/v1/chat/completions`；支持 `parentContext` 作 system 上下文。
- 验证：`tests/deepseek.test.cjs`（mock fetch，4 断言全过）：映射 content/model、请求携带 Bearer key 与正确 model、无 `parentContext` 不注入 system、非 2xx 抛状态码。**真机可用真实 key 跑通**（沙箱无外网，不在此验证真实网络请求）。
- 提交：`step B.5 deepseek direct adapter`

**B.6 示例 MCP Server** ✅（代码完成，待提交）
- 目标：一个独立进程 MCP Server，暴露 `generate` tool。
- 动作：
  - `src/core/mcp-server/workbuddy-mcp-server.ts`：用 SDK 高层 **`McpServer`**（`@modelcontextprotocol/sdk/server/mcp.js`）注册 `generate` tool（`inputSchema` 用 zod）；`createMcpServer(generator)` 不连传输层便于单测；`startStdioServer()` 走 `StdioServerTransport` 并作为可执行入口（`require.main === module` 守护）。
  - `src/core/mcp-server/sdk-modules.d.ts`：该 SDK 在本机冻结安装中缺可解析的 .d.ts，提供最小 ambient 声明（子路径导入必须带 `.js`，否则 Node 不补扩展名）。
- 验证：`tests/mcp-server.test.cjs` 用 `InMemoryTransport` 在进程内把 Client↔Server 对接，**3 断言全过**：`listTools` 暴露 `generate`（含 description + inputSchema）；`callTool generate` 返回生成文本；缺必填参数返回 `isError`（不静默成功）。
- 提交：`step B.6 example workbuddy-mcp-server`

**B.7 MCP 接入：Manager + Adapter** ✅（代码完成，待提交）
- 目标：主程序能拉起示例 Server 并成功调用其 `generate`。
- 动作：
  - `src/core/mcp/McpClientManager.ts`：用 `StdioClientTransport`（`@modelcontextprotocol/sdk/client/stdio.js`）拉起 MCP Server 子进程，维护 `id → Client`，提供 `connect / listTools / callTool / disconnect / disconnectAll`（`callTool` 拼接文本片段、`isError` 抛错）。
  - `src/core/mcp/McpAdapter.ts`：实现 `Generator`（`kind:'mcp'`），把某 Server 的某个 tool 包装成统一生成器（`generate(spec)` → `callTool` → `NodeContent`）。
  - 示例 Server 增加测试钩子 `DIVERGE_MCP_FAKE=1`（子进程用确定性假生成器，避免联网）。
- 验证：`tests/mcp-manager.test.cjs` 真实 spawn 子进程 MCP Server，**4 断言全过**：子进程暴露 `generate`；`listTools` 带 description；`McpAdapter.generate()` 经真实子进程返回文本 + contentType；`disconnectAll` 关闭连接。
- 提交：`step B.7 mcp client manager + adapter`

### 阶段 C · 界面与交互

**C.1 画布基础渲染** ✅（代码完成，待提交）
- 目标：React Flow 显示几个固定节点。
- 动作：
  - `src/renderer/canvas/CreativeTree.tsx`：用 `@xyflow/react` 渲染 3 个写死节点（创意主题 / 方向 A / 方向 B）+ 2 条动画连线，含 `Background / Controls / MiniMap`、`fitView`。
  - `src/renderer/App.tsx`：包一层 `ReactFlowProvider` 渲染画布；`src/renderer/styles.css`（full-height root）+ `main.tsx` 引入全局样式。
- 验证：`tsc` 类型检查通过；`vite build` 打包成功（194 模块，CSS 16KB 含 xyflow 样式，JS 328KB）；**并在 Electron 中实际加载构建产物截图实测** —— 3 个节点（创意主题 / 方向 A / 方向 B）+ 2 条动画连线渲染正常（见 B.1 谜团与验证方法说明）。
- 提交：`step C.1 react flow static canvas`

**C.2 节点点击 → 预览面板（文本版）** ✅（代码完成，待提交）
- 目标：点节点弹出预览，先显示纯文本/占位。
- 动作：
  - `src/renderer/store/treeStore.ts`：Zustand store（`nodes / edges / selectedNodeId` + `onNodesChange / onEdgesChange / onConnect / selectNode`），用 `@xyflow/react` 的 `applyNodeChanges / applyEdgeChanges / addEdge`；画布改由 store 驱动。
  - `src/renderer/panels/PreviewPanel.tsx`：右侧固定面板，选中节点显示 `label + content`，未选中显示提示。
  - `App.tsx`：flex 布局（画布 + 面板）；`CreativeTree` 接 `onNodeClick / onPaneClick`。
- 验证：`tsc` + `vite build` 通过；**Electron 里派发真实鼠标点击**（`sendInputEvent`）后读取面板文本：`点击前="点击画布中的节点查看内容"` → `点击「方向 A」后="方向 A / 方向 A 的初步想法：…"`，并截图确认。
- 提交：`step C.2 node click + preview panel (text)`

**C.3 生成对话框 + IPC 打通 ✅**
- 目标：选后端 + 提示词 → 经主进程生成 → 落库 → 节点显示。
- 动作（已落地）：
  - 共享契约 `src/shared/ipc.ts`：频道名 `IPC`、`GeneratorInfo` / `GenerateNodeRequest` / `GenerateNodeResult` / `DivergeApi`。
  - 主进程：`main/ipc/handlers.ts`（`listGenerators` / `ensureProject` / `generateNode` 三个 handler）、`main/preload.ts`（`contextBridge` 暴露 `window.diverge`，不暴露 ipcRenderer 本体）、`main/app.ts`（`bootstrap()` 装配服务→handler→窗口，`main.ts` 只调用它）。
  - 核心逻辑抽成纯函数 `src/core/generate-node.ts`（可 Node 单测）；`core/services.ts` 组装仓储 + 生成器注册表，无 DeepSeek key 时用 `createFakeGenerator` 兜底。
  - 渲染端：`store/treeStore.ts` 增加 `init()`（拉项目+生成器）、`openDialog/closeDialog/generate()`；新增 `dialogs/GenerateDialog.tsx`；`App.tsx` 加顶栏「＋ 新增想法」；`PreviewPanel` 加「从这里发散」。
- 关键坑（已解决）：**Electron 默认 `sandbox: true` 会禁止 preload 做相对路径 require**（只允许 `require('electron')`），导致 `require('../shared/ipc')` 报 `module not found`、`window.diverge` 永不注入。已在 `app.ts` 设 `sandbox: false`（保留 `contextIsolation` + `nodeIntegration:false`），换来 preload 直接复用 `shared/ipc` 契约。**渲染端有无主进程都能工作**，所以这次故障表现为"静默降级"而非报错——正是探针读 `Object.keys(window.diverge)` 才抓到。
- 另修：新增节点后自动 `fitView`，避免新节点落在视口外（初次 `fitView` 只覆盖初始节点）。
- 验证：
  - `tests/generate-node.test.cjs` **10 断言**（生成/落库/父上下文推断/显式上下文优先/根层无边/label 截断/未知生成器报错）。
  - **端到端探针**（Electron 跑生产 `bootstrap()`，假生成器）**26 断言全过**：`window.diverge` 注入 → 初始 1 节点 → 打开对话框 → 选生成器 → 输入 → 生成 → 画布 2 节点 → 预览显示内容 → 磁盘文件为真；再从选中节点「从这里发散」→ 3 节点 + 1 边 → 子节点内容含"基于父节点" → 磁盘 parentId/edge 正确；并截图确认。
- 说明：真实 DeepSeek 调用需 key + 联网，沙箱内用假生成器验证链路本身；`DIVERGE_FAKE_GENERATOR=1` 即启用兜底生成器。
- 提交：`step C.3 generate dialog + ipc end-to-end`

**C.4 多类型预览**
- 目标：HTML / Markdown / SVG 各自正确渲染。
- 动作：`PreviewPanel` 分支：html→沙箱 iframe(srcDoc)；markdown→ReactMarkdown；svg→安全渲染。
- 验证：分别用三类样例内容验证渲染正确、脚本不越权。
- 提交：`step C.4 multi-type preview (html/md/svg)`

**C.5 发散/收敛交互**
- 目标：从节点继续发散生成子节点，旧版保留可翻案。
- 动作：节点右键"发散"→ 打开 GenerateDialog（默认父节点上下文）；生成 N 个子节点（限流并发）；保留历史版本。
- 验证：从节点 A 发散出子节点，树结构正确；旧节点仍在，可切回对比。
- 提交：`step C.5 diverge/converge interaction`

**C.6 AI 后端管理 UI**
- 目标：可视化配置 DeepSeek key、添加/启停 MCP server。
- 动作：`panels/AISourcePanel.tsx`；IPC `LIST_AI_SOURCES` / `ADD_MCP_SERVER`；配置持久化到 `ai_source` / `mcp_server` 表。
- 验证：填入 key 并保存后能用于生成；添加示例 MCP server 后出现在后端列表且可连。
- 提交：`step C.6 ai source management ui`

### 阶段 D · 收尾

**D.1 导出功能**
- 目标：最终方案导出 Markdown / 单文件 HTML。
- 动作：`panels/ExportPanel.tsx` + IPC `EXPORT`；Markdown 直接写；HTML 内联 CSS/JS 单文件。
- 验证：导出文件生成，双击/浏览器打开内容正确。
- 提交：`step D.1 export markdown + single-file html`

**D.2 Windows 打包**
- 目标：产出可安装的 exe/nsis。
- 动作：`electron-builder.yml`（appId/productName/win-nsis/icon）；`package.json` 加 `build` 脚本。
- 验证：本机 `pnpm build` 产出安装包；可安装并运行，功能与 dev 一致。
- 提交：`step D.2 windows packaging (nsis)`

### 阶段 E · 上传（按"确认后上传"）

- **E.1** 阶段 A 完成并你确认后：`git push -u origin main`
- **E.2** 阶段 B/C/D 各自完成并确认后：`git push origin main`
- 每次 push 前我口头汇报"即将推送哪些提交"，你确认后执行。

---

## 4. 验证方法汇总

| 类型 | 方法 |
|---|---|
| 启动类（B.1/C） | 运行 `pnpm dev`，肉眼确认窗口/画布现象 |
| 存储类（B.2/B.3） | 启动后检查 `diverge.db` 文件；临时脚本插入读回 |
| 纯类型（B.4） | `tsc --noEmit` 通过 |
| AI 类（B.5/B.6/B.7） | 临时脚本/Inspector 调 `generate`，打印返回文本 |
| 交互类（C.3/C.5） | 界面操作 → 观察节点/树/库变化 |
| 打包类（D.2） | 安装包安装后运行验证 |

---

## 5. 风险与回滚

- **依赖/原生模块问题**（better-sqlite3、MCP SDK 需 Electron ABI 重建）：遇 ABI 错误先 `electron-rebuild`，不绕过。
- **Shell 环境异常**：本机 Git Bash/PowerShell 输出捕获不稳定，执行 git 操作若异常，改用 WorkBuddy 内置 git 能力或让你在终端手动跑，我不瞎猜结果。
- **每步可回退**：任一步失败立即停，汇报；必要时 `git revert <step-commit>` 回到上一步干净态。
- **密钥安全**：DeepSeek key 只存本地 `userData`/数据库，绝不进 `.env` 或提交；`.gitignore` 已屏蔽。

---

> 下一步建议从 **A.0 仓库初始化** 开始。我会先做环境前置检查（第 2 节），再执行 A.0，完成后汇报并等你确认再继续。

---

## 6. 变更记录（决策偏离）

- **2026-09-20 · 存储引擎调整**：原计划 B.2/B.3 使用 `better-sqlite3`（SQLite 文件）。实测发现：(1) 本机 `pnpm` 因 Windows 符号链接权限失败（已统一改用 `npm`）；(2) `better-sqlite3` 原生二进制未构建成功，且其在 Electron 中需针对 Electron 的 Node ABI 重编译（沙箱屏蔽相关构建工具，无法稳定完成）。**改为纯 JS 的 JSON 文件存储**（每个项目一个 `userData/diverge/projects/<id>.json`），零原生依赖、绝对可靠、天然可导出。数据模型（project/tree/node/edge）不变。详见方案文档对应变更。
- **A.2 已完成并提交**（commit `98fd27c`）：依赖树现为纯 JS（react / @xyflow/react / zustand / @modelcontextprotocol/sdk / react-markdown / electron / vite / typescript / electron-builder / concurrently），无 `.node` 原生模块残留。
- **B.1 已完成并提交**（commit `c54db8c`）：Electron 主进程 + 渲染占位页代码完成；`tsc` 类型检查与 `build:main` 编译均已验证通过。窗口弹出验证因沙箱无 GUI 暂缓，需在用户 Windows 真机 `npm run dev` 最终确认（`require('electron')` 在沙箱内不返回 API 的环境限制）。
- **B.2 / B.3 已完成（待提交）**：JSON 存储层（`JsonStore` + `ProjectRepository` + 共享类型 `src/shared/types.ts`）落地，`tests/storage.test.cjs` 11 项断言全过。新增 `tsconfig.test.json`（编译 `src/core`+`src/shared` 至 `dist-test` 供 Node 测试）、`tests/storage.test.cjs`；`.gitignore` 增加 `dist-test/`；`tsconfig.main.json` 的 `include` 扩展为 `["src/main","src/core","src/shared"]` 以便主进程直接编译存储层。
- **B.4 已完成（待提交）**：`src/core/generator/types.ts` 定义统一 `Generator` 接口与 `GeneratorRegistry`，为 B.5（DeepSeek 直连）与 B.7（MCP adapter）提供适配接缝；`tsc` 编译通过。
- **B.5 已完成（待提交）**：`src/core/generator/direct/deepseek.ts`（`DeepSeekGenerator`）实现 `Generator`，`fetch` 直连 `/v1/chat/completions`，apiKey 注入式；`tests/deepseek.test.cjs`（mock fetch）4 断言全过。
- **B.6 已完成（待提交）**：示例 MCP Server（`McpServer` + `generate` tool + `startStdioServer`），`tests/mcp-server.test.cjs`（InMemoryTransport 端到端）3 断言全过。
- **B.7 已完成（待提交）**：`src/core/mcp/McpClientManager.ts`（`StdioClientTransport` 拉子进程）+ `src/core/mcp/McpAdapter.ts`（tool → `Generator`，`kind:'mcp'`）；示例 Server 增 `DIVERGE_MCP_FAKE` 测试钩子；`tests/mcp-manager.test.cjs`（真实子进程）4 断言全过。**至此阶段 B 全部完成，累计 22 项断言全过。**
- **C.1 已完成（待提交）**：画布基础渲染（`CreativeTree` + `ReactFlowProvider` + 全局样式）。`tsc` + `vite build` 均通过，并已用 Electron 截图实测（3 节点 + 2 连线）。
- **C.2 已完成（待提交）**：Zustand `treeStore`（节点/边/选中态，画布由 store 驱动）+ `PreviewPanel`（右侧面板）；`tsc` + `vite build` 通过；**Electron 真实鼠标点击验证**（点击前提示 → 点击「方向 A」后面板显示其内容）。
- **C.3 已完成（待提交）**：`shared/ipc.ts` 契约 + 主进程 IPC handlers/preload/`app.ts(bootstrap)` + `core/generate-node.ts` + 渲染端 `GenerateDialog` 与 store 接线。`tests/generate-node.test.cjs` **10 断言**、Electron 生产路径端到端探针 **26 断言**全过。**累计 5 个测试套件 32 项断言全过**（storage 11 + deepseek 4 + mcp-server 3 + mcp-manager 4 + generate-node 10）。
- **验证方法固化**：C 阶段 UI 统一用「清 `ELECTRON_RUN_AS_NODE` → Electron 加载 `dist/renderer/index.html` → `executeJavaScript` 读 DOM/计算样式 + `sendInputEvent` 真实交互 + `capturePage` 截图」验证。
- **✅ B.1 谜团已解（重要）**：`npm run dev` 早先报 `electron_1.app is undefined`、`require('electron')` 返回字符串 —— 根因是本机 shell 环境设了 **`ELECTRON_RUN_AS_NODE=1`**，使 `electron.exe` 以纯 Node 模式运行（不加载 Electron 内置模块）。**清掉该变量后 Electron 完全正常**（探针打印 `typeof app = object` / `ELECTRON_APP_READY_OK`）。结论：(1) **B.1 的窗口在真机（无此变量）本来就是正常的**；(2) 因此沙箱内也能做 UI 视觉验证 —— 清掉 `ELECTRON_RUN_AS_NODE` 后用 Electron 加载构建产物、`webContents.capturePage()` 截图。C.1 即以此法完成视觉确认。后续 C 阶段沿用此法。
- **环境修复（补记 2026-09-20，node_modules 不入库）**：发现顶层 npm 拷贝**大面积不完整**（先在 `@xyflow/react` 暴露：缺 `types/nodes.d.ts`、`store.d.ts` 等）。已做**通用修复**：对每个非符号链接的顶层包，从其 `.pnpm` 完整副本补齐缺失文件（只增不改）——共修 **29 个包、625 个文件**（含 `@modelcontextprotocol/sdk` 371 个 .d.ts、`typescript`、`vite`）。**重装依赖后需重新复核此修复。**
- **环境修复（2026-09-20，node_modules 不入库）**：本机 `node_modules` 是「pnpm store（完整）+ 顶层不完整 npm 拷贝」的混合体。顶层 `node_modules/zod` 缺 `v3/v4/v4-mini` 子包（`@modelcontextprotocol/sdk` 运行时依赖），已从完整 store `.pnpm/zod@4.6.5/node_modules/zod` 拷贝补齐；MCP SDK 顶层缺 `.d.ts`，以 `src/core/mcp-server/sdk-modules.d.ts` 的 ambient 声明兜底（因本工程 tsconfig 用 node10 解析、不读 exports 映射）。**若日后重装依赖，需复核这两处是否被正确还原。**
